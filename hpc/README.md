# HPC job scripts

This folder contains job submission scripts for running the project code in a High-Performance Computing (HPC) environment using [slurm](https://slurm.schedmd.com/overview.html). These workflows are setup to take advantage of parallel processing.
